<UserControl x:Class="MVVMFIleManagerWPF.CustomControls.FileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFIleManagerWPF.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Image x:Name="btnImage" Source="{Binding Icon}" Grid.Column="0"/>
        <TextBlock x:Name="txtFileNumber" Text="{Binding Path=Text}" Grid.Column="1"/>
        <TextBlock x:Name="txtFileType" Text="{Binding Name}" Grid.Column="3"/>
        <TextBlock x:Name="txtDate" Text="{Binding DateCreated}" Grid.Column="4"/>
        <TextBlock x:Name="txtExtension" Text="{Binding Extension}" Grid.Column="5"/>
        <Image x:Name="btnImageOptions" Source="{Binding Path=ImageOptions}" Grid.Column="6"/>
        <Image x:Name="btnImageAdd" Source="{Binding Path=ImageAdd}" Grid.Column="7"/>
        <Image x:Name="btnImageShare" Source="{Binding Path=ImageShare}" Grid.Column="8"/>
    </Grid>
</UserControl>
