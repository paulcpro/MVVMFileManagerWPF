<UserControl x:Class="MVVMFIleManagerWPF.CustomControls.SearchBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFIleManagerWPF.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <TextBlock Margin="10,0,0,0" x:Name="txtText1" Text="{Binding Path=Text1}" Foreground="#A2A2A3" VerticalAlignment="Center" Grid.Column="0"/>
        <Image x:Name="imgSource" Source="{Binding Path=ImageSource}" Grid.Column="2" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
