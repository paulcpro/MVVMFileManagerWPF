<UserControl x:Class="MVVMFIleManagerWPF.CustomControls.Stats"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFIleManagerWPF.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="150">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="txtFirst" Width="70" Text="{Binding FirstText}" FontWeight="Bold" FontSize="14" Grid.Column="0"/>
        <TextBlock x:Name="txtNumber" HorizontalAlignment="Center" Text="{Binding Number}" Grid.Column="1" FontSize="20"/>
        <TextBlock x:Name="txtSecond" Text="{Binding SecondText}" Grid.Column="2"/>
    </Grid>
</UserControl>
